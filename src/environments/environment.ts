// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase: {
    apiKey: 'AIzaSyAAXjeWsD_6hPYO_aFk_Wa2kcu-defSQSM',
    authDomain: 'angular-github-auth.firebaseapp.com',
    databaseURL: 'https://angular-github-auth.firebaseio.com',
    projectId: 'angular-github-auth',
    storageBucket: 'angular-github-auth.appspot.com',
    messagingSenderId: '1047062870960',
    appId: '1:1047062870960:web:f4a4ba740193732ff53475',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
